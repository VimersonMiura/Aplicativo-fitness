# BJJ Treinos

PWA para montar treinos de musculação e condicionamento físico voltados ao jiu jitsu. Dados salvos localmente no navegador.

## Rodar

```
nvm use 22
npm install
npm run dev
```

`npm run build` gera a versão instalável (PWA) em `dist/`.
